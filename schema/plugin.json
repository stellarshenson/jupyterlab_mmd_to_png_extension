{
  "title": "Mermaid to PNG Extension",
  "description": "Settings for Mermaid diagram PNG export",
  "jupyter.lab.setting-icon": "ui-components:image",
  "jupyter.lab.setting-icon-label": "Mermaid to PNG",
  "type": "object",
  "properties": {
    "targetDPI": {
      "title": "Target DPI",
      "description": "Resolution for exported PNG images (dots per inch). Higher values produce larger, higher-quality images suitable for printing. Standard values: 96 (screen), 150 (draft print), 300 (standard print), 600 (high-quality print), 1200 (professional print).",
      "type": "number",
      "default": 300,
      "minimum": 72,
      "maximum": 1200
    },
    "transparentBackground": {
      "title": "Transparent Background",
      "description": "Export PNG images with transparent background. When disabled, uses white background.",
      "type": "boolean",
      "default": true
    },
    "mermaidTheme": {
      "title": "Mermaid Theme",
      "description": "Theme for Mermaid diagram rendering. Leave empty to use notebook theme (recommended). Options: default, dark, forest, neutral, base.",
      "type": "string",
      "default": "",
      "enum": ["", "default", "dark", "forest", "neutral", "base"]
    }
  },
  "additionalProperties": false
}
